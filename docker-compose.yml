mongodb:
  image: mongo:latest
  ports:
    - "34017:27017"
  environment:
    MONGO_INITDB_ROOT_USERNAME: "your_username" # Replace with your desired username
    MONGO_INITDB_ROOT_PASSWORD: "your_password" # Replace with your desired password